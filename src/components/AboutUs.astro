---
// Importamos los mismos datos para no tener que escribirlos dos veces
import profiles from '../data/profiles.json';
---

<section id="sobre-nosotros" class="relative overflow-hidden bg-white py-24 sm:py-32">
  
  <div class="absolute -top-24 -left-20 -z-10 h-96 w-96 rounded-full bg-rose-100 blur-3xl opacity-50"></div>
  <div class="absolute top-1/2 right-0 -z-10 h-96 w-96 rounded-full bg-indigo-100 blur-3xl opacity-50"></div>

  <div class="mx-auto max-w-7xl px-6 lg:px-8">
    
    <div class="mx-auto max-w-2xl text-center mb-16">
      <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">Sobre Nosotros</h2>
      <p class="mt-6 text-lg leading-8 text-gray-600">
        Somos un colectivo de <span class="font-bold text-indigo-500">profesionales independientes</span> unidos por una misma pasión: la excelencia. 
      </p>
      <p class="mt-4 text-base leading-7 text-gray-500">
        Creemos que el mejor servicio nace de la especialización y el trato directo. Sin intermediarios, sin burocracia. 
        Cada uno de nosotros es experto en su área, trabajando de la mano para ofrecer soluciones integrales, personalizadas y de la más alta calidad a nuestro público.
      </p>
    </div>

    <ul role="list" class="mx-auto grid max-w-2xl grid-cols-1 gap-x-8 gap-y-12 sm:grid-cols-2 lg:mx-0 lg:max-w-none lg:grid-cols-3">
      
      {profiles.map((profile) => (
        <li class="group">
          <div class="relative flex flex-col items-center rounded-2xl bg-gray-50/50 p-8 text-center border border-gray-100 shadow-sm transition-all duration-300 hover:shadow-lg hover:-translate-y-1 hover:bg-white">
            
            <div class="relative mb-6">
                <div class="absolute -inset-1 rounded-full bg-gradient-to-r from-rose-200 via-purple-200 to-indigo-200 opacity-70 blur transition duration-300 group-hover:opacity-100"></div>
                <img 
                  class="relative h-32 w-32 rounded-full object-cover border-4 border-white shadow-sm" 
                  src={profile.photoUrl} 
                  alt={profile.name} 
                />
            </div>

            <h3 class="mt-2 text-xl font-bold tracking-tight text-gray-900 group-hover:text-indigo-600 transition-colors">
              {profile.name}
            </h3>
            <p class="text-sm font-semibold leading-6 text-indigo-500 uppercase tracking-wide">
              {profile.role}
            </p>

            <div class="mt-4 h-1 w-12 rounded-full bg-gray-200 group-hover:bg-gradient-to-r group-hover:from-rose-300 group-hover:to-indigo-300 transition-all duration-300"></div>
            
          </div>
        </li>
      ))}

    </ul>
  </div>
</section>